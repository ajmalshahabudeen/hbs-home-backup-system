POSTGRES_USER=hbs
POSTGRES_PASSWORD=hbs_secret_change_me
POSTGRES_DB=hbs_backup
POSTGRES_PORT=5432

# Host machine (apps + prisma CLI)
DATABASE_URL="postgresql://hbs:hbs_secret_change_me@localhost:5432/hbs_backup?schema=public"

# Inside docker compose network, server uses:
# DATABASE_URL="postgresql://hbs:hbs_secret_change_me@postgres:5432/hbs_backup?schema=public"
