node_modules
# Keep environment variables out of version control
.env

# generated client is produced by `prisma generate` (keep out of git if desired;
# Docker build runs generate). Uncomment to ignore:
# /generated
